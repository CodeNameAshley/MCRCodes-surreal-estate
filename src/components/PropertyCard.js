import React from "react";

export default function PropertyCard() {
  // { bathrooms, bedrooms, city, email, price, title, type } = Variable
  return (
    <div className="property-card">
      {/* {title}
            {type} - {city}
            {} */}
    </div>
  );
}
